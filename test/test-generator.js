var chai = require('chai');
var expect = chai.expect;
var should = chai.should();
var mda = require('../MultiDimensional_Arrays.js');
 
 
describe('generate function will return 3 arrays with a length of 3', function() {
  it('should do something', function() {
    var something = "some value";
    expect(something).to.be.a('string');
    something.should.equal('some value');
  });
});